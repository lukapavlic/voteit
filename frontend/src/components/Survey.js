import React from 'react'

const Survey = ({survey}) => {
    return (
        <div>
            <h3>{survey.title}</h3>
            <p>
                options
            </p>
        </div>
    )
}

export default Survey
